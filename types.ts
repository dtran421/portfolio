export type SubsectionObject = {
    title: string;
    organization: string;
    date: string;
    description: string;
};

export type EventObject = {
    heading: string;
    type: string;
    date: string;
    body: string;
};
