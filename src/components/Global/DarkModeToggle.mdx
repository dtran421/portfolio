{/* DarkModeToggle.mdx */}

import { Canvas, Stories, Meta } from "@storybook/blocks";

import * as DarkModeToggleStories from "./DarkModeToggle.stories";

<Meta of={DarkModeToggleStories} />

# DarkModeToggle

The DarkModeToggle component is used to toggle between light and dark modes.

_Note that the following story for dark mode is broken (the toggle isn't properly switched) as Storybook doesn't support programmatic dark mode._

<Stories />
