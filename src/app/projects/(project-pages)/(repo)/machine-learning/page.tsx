import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Machine Learning",
};

const MachineLearningPage = () => (
  <>
    This class serves as an introduction to the theory and practice of machine learning, focusing primarily on methods
    for classification and prediction. Topics include decision trees, artificial neural networks, support vector
    machines, kernel methods, ensemble methods, clustering methods, dimension reduction, performance evaluation, data
    preprocessing, and hyperparameter tuning.
  </>
);

export default MachineLearningPage;
